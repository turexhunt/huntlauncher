<!-- src/HuntLoader/Views/Dialogs/UpdateDialog.xaml -->
<Window x:Class="HuntLoader.Views.Dialogs.UpdateDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Доступно обновление"
        Width="480" Height="320"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">

    <Border Background="#1A1A2E" CornerRadius="12"
            BorderBrush="#FF6B35" BorderThickness="1">
        <Grid Margin="24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Заголовок -->
            <TextBlock Grid.Row="0"
                       Text="🚀 Доступно обновление!"
                       FontSize="20" FontWeight="Bold"
                       Foreground="#FF6B35" Margin="0,0,0,8"/>

            <!-- Версия — берётся из кода, не хардкод -->
            <TextBlock Grid.Row="1"
                       x:Name="VersionText"
                       FontSize="14" Foreground="#AAAAAA"
                       Margin="0,0,0,12"/>

            <!-- Changelog -->
            <ScrollViewer Grid.Row="2" Margin="0,0,0,16">
                <TextBlock x:Name="ChangelogText"
                           FontSize="13" Foreground="#CCCCCC"
                           TextWrapping="Wrap"/>
            </ScrollViewer>

            <!-- Прогресс бар -->
            <StackPanel Grid.Row="3" x:Name="ProgressPanel"
                        Visibility="Collapsed" Margin="0,0,0,12">
                <TextBlock x:Name="ProgressText"
                           Text="Скачиваю..." Foreground="#AAAAAA"
                           FontSize="12" Margin="0,0,0,6"/>
                <Border Background="#0F0F1A" CornerRadius="4" Height="8">
                    <Border x:Name="ProgressFill" Background="#FF6B35"
                            CornerRadius="4" HorizontalAlignment="Left"
                            Width="0"/>
                </Border>
            </StackPanel>

            <!-- Кнопки -->
            <StackPanel Grid.Row="4" Orientation="Horizontal"
                        HorizontalAlignment="Right">
                <Button x:Name="SkipBtn" Content="Пропустить"
                        Width="120" Height="36" Margin="0,0,12,0"
                        Background="#2A2A3E" Foreground="#AAAAAA"
                        BorderThickness="0" Cursor="Hand"
                        Click="SkipBtn_Click">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Background" Value="#2A2A3E"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}"
                                                CornerRadius="8">
                                            <ContentPresenter
                                                HorizontalAlignment="Center"
                                                VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                </Button>

                <Button x:Name="UpdateBtn" Content="Обновить сейчас"
                        Width="140" Height="36"
                        Background="#FF6B35" Foreground="White"
                        BorderThickness="0" Cursor="Hand"
                        Click="UpdateBtn_Click">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}"
                                                CornerRadius="8">
                                            <ContentPresenter
                                                HorizontalAlignment="Center"
                                                VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                </Button>
            </StackPanel>
        </Grid>
    </Border>
</Window>